# 설계 v1.00 대비 구현 상태

기준: docs/FCC.md(2026-09-10), 백엔드 v0.2. 로컬에서 실행 가능한 구현과 AWS 연결 코드를 구분합니다.

| 요구사항 | 코드 구현 | 검증/비고 |
|---|---|---|
| 가입·로그인·로그아웃·프로필 | 구현 | 로컬 HTTP 테스트, Cognito 어댑터 모의 테스트 |
| 그룹 생성·초대·참여·구성원·탈퇴 | 구현 | 이메일 지정/일회용 초대, 소유권 이전, 배정 중 탈퇴 방지 |
| 고령자 등록·기본정보·수정·그룹 연결 | 구현 | 이름·생년월일·특이사항, 별도 등록 지원 |
| 현재/다음 담당자·교대·이력 | 구현 | 담당자 권한 및 원자적 교대 |
| 기록 CRUD·유형·작성자·기간/유형 Timeline | 구현 | 작성자만 수정/삭제, 시스템 이력 보호 |
| 병원·검사·방문·기타 일정 CRUD | 구현 | 직접 취소, 취소와 삭제 구분 |
| 오늘/다음 일정·담당자 지정 | 구현 | 서울 날짜, 상태 및 시간 필터 |
| 자연어 분석·유형 분류·정보 추출·저장 | 구현 | Bedrock 연동 코드, 로컬 규칙 모드; 실제 모델 평가 미실시 |
| 인수인계 생성·요약·조회·이력·재생성 | 구현 | 기간·근거·원문 스냅샷, 새 ID로 재생성 |
| 교대 시 인수인계·다음 보호자 확인 | 구현 | 확인자/확인일 저장, 기록 없으면 교대만 |
| AI 실패·빈 기록 예외 | 구현 | 502/422, 교대 롤백, 원문 보존 |
| Handler → Service → Repository | 구현 | http/lambda, services, repositories, adapters 분리 |
| DynamoDB·Cognito·Lambda·API Gateway | 코드/템플릿 구현 | AWS 계정 배포 전; 실제 클라우드 통합 미검증 |
| 프론트 화면 | 별도 저장소 | 이번 backend 작업 범위 밖 |

## 작업 순서

1. 로컬 실행 후 가입 → 그룹 생성 → 고령자 등록 → 초대/참여 → 기록 → 일정 → 교대 → 인수 확인 흐름을 프론트에 연결합니다.
2. AWS CLI 계정을 연결하고 SAM으로 서울 리전에 DB·인증·API를 생성합니다. DATABASE.md의 배포 명령을 사용합니다.
3. Bedrock 사용 가능한 모델을 선택해 모델 ID/ARN을 설정하고 실제 한국어 기록으로 의미적 정확도·지연·비용·실패 처리를 평가합니다.
4. 실제 AWS에서 가족 간 접근, 초대 수락, 로그아웃 토큰 차단, 동시 승인/교대, 장애 시 데이터 보존을 확인합니다.
5. 장기 운영 전에 그룹 aggregate를 기록/인수인계별 DynamoDB 항목으로 분리하고 페이지네이션을 추가합니다. 현재 그룹당 350 KB 한도는 소규모 MVP용입니다.

## 확장 범위

설계서 2.5의 승인 요청과 제한된 DB 텍스트 질문은 유지했습니다. 승인 절차는 보호자의 일반 일정 취소에 강제하지 않습니다. 알림 발송/확인함, Transcribe/Polly 음성, 위치/생활 패턴 감지는 MVP 이후 작업입니다.

설계 표의 이름별 ID는 API에서는 공통 `id`로 표현하고 사용자 ID는 `userId`를 유지합니다. 기존 v0.1 API와 데이터 변경점은 MIGRATION.md에 있습니다.
